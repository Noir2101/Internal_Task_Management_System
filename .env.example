# ITMS backend — mẫu biến môi trường. Copy sang `.env` rồi điền giá trị thật.

DATABASE_URL="postgresql://itms:itms@localhost:5433/itms?schema=public"

# JWT_ACCESS_SECRET đủ dài + ngẫu nhiên; access TTL ngắn.
JWT_ACCESS_SECRET="thay-bang-chuoi-ngau-nhien-du-dai"
JWT_ACCESS_TTL="15m"
REFRESH_TTL_DAYS="7"
# Chỉ dùng cho e2e của frontend. KHÔNG bật ở prod.
# THROTTLE_DISABLED="false"

# Redis (GĐ11). MỘT biến bật hai thứ:
#   - slice 1: store cho bộ đếm throttle. Bỏ trống -> in-memory (đủ cho dev và test một tiến trình);
#     có giá trị -> mọi replica backend đếm chung, giới hạn không nở theo số instance.
#   - slice 2: queue thông báo. Bỏ trống -> EmailNotifier gửi đồng bộ trong đường request và KHÔNG có
#     digest quá hạn; có giá trị -> việc gửi rời sang worker, và lịch digest nằm ở Redis.
# docker-compose tự đặt "redis://redis:6379"; máy dev thường không cần biến này.
# REDIS_URL="redis://localhost:6379"

# Lịch digest quá hạn gửi cho leader (GĐ11 slice 2), dạng cron, múi giờ UTC.
# Mặc định 01:00 UTC tức 08:00 giờ Việt Nam. Chỉ có tác dụng khi REDIS_URL có giá trị.
# OVERDUE_DIGEST_CRON="0 1 * * *"

# MAIL_ENABLED=true → EmailNotifier (nodemailer SMTP). Thiếu SMTP_*/MAIL_FROM khi true → fail-fast.
MAIL_ENABLED="false"
# Địa chỉ gửi. Demo Resend chưa verify domain: dùng "onboarding@resend.dev".
MAIL_FROM="ITMS <onboarding@resend.dev>"
# Provider prod = Resend (đổi provider = đổi các dòng SMTP_*).
SMTP_HOST="smtp.resend.com"
SMTP_PORT="465"
SMTP_SECURE="true"
SMTP_USER="resend"
# Resend API key (bắt đầu bằng "re_").
SMTP_PASS=""
