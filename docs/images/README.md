# images/

Ảnh minh hoạ cho tài liệu và README.

- `swagger-*.png` — chụp trang Swagger `/api/v1/docs` khi chạy `docker compose up`: envelope lỗi,
  cờ OVERDUE suy ra, endpoint stats, lỗi validation.
- Sơ đồ thường vẽ bằng Mermaid thẳng trong `.md`; chỉ đặt file ảnh ở đây khi cần bản chụp thật.
